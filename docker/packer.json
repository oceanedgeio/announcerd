{
  "builders": [
    {
      "type": "docker",
      "image": "node:12-alpine",
      "commit": "true",
      "changes": ["CMD [\"npm\", \"/start\"]"]
    }
  ],
  "provisioners": [
    {
      "type": "file",
      "source": "discord-announcer.tar.gz",
      "destination": "/discord-announcer.tar.gz"
    },
    {
      "type": "shell",
      "inline": ["tar -zxf discord-announcer.tar.gz", "npm install --production", "npm run build"]
    }
  ],
  "post-processors": [
    [
      {
        "type": "docker-tag",
        "repository": "egeeio/discord-announcer",
        "tag": "latest"
      },
      "docker-push"
    ]
  ]
}
